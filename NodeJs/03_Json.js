/**
 * JSON is javascript object notation. represent as a key-value pair but key must be wrapped in string,
 * and in javascript object you can use function.
 * 
 * 
 * Web-Application: send html data as a response
 * Web-API: send JSON data as a response
 * 
 * response header type:
 *  'Content-Type' :  "application/json"
 */